package org.asgs.twitterfeeds.common.clients;
// Purpose of this enum to keep the number of rows to be retrieved to minimal.
public enum ResultType {
  ALL, // Return all results.
  FIRST // Return the first result.
}
